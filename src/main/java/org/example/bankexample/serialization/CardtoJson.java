package org.example.bankexample.serialization;

import com.google.gson.Gson;
import com.google.inject.Inject;
import org.example.bankexample.accounts.BankAccount;
import org.example.bankexample.cards.Card;

import java.util.Map;
import java.util.Set;

public class CardtoJson
{
    /*@Inject
    private Gson gson;
    public String GsonSerilazeCard(Map cards)
    {
        return this.gson.toJson(cards);
    }*/
}
